/** @jsx React.DOM */
var React = require('react');

var Home =
    React.createClass({
        render:function() {
            return <div>Home Screen(test jenkins)</div>
        }
    });

module.exports = Home;